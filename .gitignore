# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright (L2) run artifacts — the committed *-snapshots baselines are kept.
playwright-report
test-results
/blob-report
.playwright
# Dev-machine visual baselines are NOT the source of truth — only CI Linux
# (-linux.png) baselines are committed. Locally-generated -darwin snapshots are
# ignored so they never pollute the repo (see e2e/visual.spec.ts header).
e2e/**/*-darwin.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Lighthouse CI reports (generated)
.lighthouseci
